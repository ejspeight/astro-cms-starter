# =============================================================================
# ENVIRONMENT VARIABLES — Copy this file to .env and fill in your values.
# =============================================================================
#
# .env is gitignored — never commit real credentials to version control.
# On Netlify, set these under Site Settings → Environment Variables.
#
# Variables prefixed PUBLIC_ are exposed to the browser (client-side).
# Variables without that prefix are server-side only (build time).
# =============================================================================

# -----------------------------------------------------------------------------
# ANALYTICS
# Set one of these — GTM is preferred (manage GA inside the container).
# Leave blank to disable analytics entirely.
# -----------------------------------------------------------------------------

# Google Tag Manager container ID (e.g. GTM-XXXXXXX)
PUBLIC_GTM_ID=

# Google Analytics 4 measurement ID (e.g. G-XXXXXXXXXX)
# Only used if PUBLIC_GTM_ID is not set.
PUBLIC_GA_ID=

# -----------------------------------------------------------------------------
# DECAP CMS / NETLIFY IDENTITY
# No env vars needed for Netlify Identity — it is configured via netlify.toml
# and the Netlify Identity widget script in BaseLayout.astro.
#
# If you switch to a self-hosted Git Gateway backend, add credentials here.
# -----------------------------------------------------------------------------

# Git Gateway backend (only needed for non-Netlify deployments)
# DECAP_BACKEND_URL=https://your-git-gateway-url.com

# -----------------------------------------------------------------------------
# FUTURE / CUSTOM INTEGRATIONS
# Add project-specific variables below as needed.
# Keep naming consistent: PUBLIC_ prefix for browser-visible values,
# no prefix for server/build-time secrets.
# -----------------------------------------------------------------------------
