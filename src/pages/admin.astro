---
/**
 * Admin Page — Serves the Decap CMS panel.
 *
 * Since trailingSlash is "ignore", this page can be served at /admin
 * (without trailing slash), which breaks Decap's relative config.yml lookup.
 * We inject a <head> <link> to tell Decap where the config lives.
 */
---

<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex" />
    <title>Content Manager</title>

    <!-- Netlify Identity Widget — handles CMS authentication -->
    <script is:inline src="https://identity.netlify.com/v1/netlify-identity-widget.js"></script>

    <!-- Set base href so Decap CMS resolves config.yml relative to /admin/ -->
    <base href="/admin/" />
  </head>
  <body>
    <script is:inline src="https://unpkg.com/decap-cms@^3.0.0/dist/decap-cms.js"></script>
  </body>
</html>
